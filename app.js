window.onload = () => {
    let heading = document.createElement('h1');
    let text = documnet.createTextNode('Document object modelling');
    heading.appendChild(text);
    document.body.appendChild(heading);
}